{{! navigation template}}
<nav id="site-nav">
  <div class="Container-Large">
    <a href="{{@blog.url}}"><h1>{{@blog.title}}</h1></a>
    <ul>
      {{#foreach navigation}}
        <a href="{{url absolute='true'}}"><li>{{label}}</li></a>
      {{/foreach}}
    </ul>
  </div>
</nav>
