{{! navigation template}}
<nav id="site-nav">
  <div class="Container-Large">
    <h1>{{@blog.title}}</h1>
    <ul>
      {{#foreach navigation}}
        <a href="{{url absolute="true"}}"><li>{{label}}</li></a>
      {{/foreach}}
    </ul>
  </div>
</nav>
